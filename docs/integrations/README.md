# Integrations

Copy-pasteable snippets for patterns the demo already proves.

| File | Pattern | Key props |
|------|---------|-----------|
| [`form-submit.md`](./form-submit.md) | Wiring into a `<form>` with `onSubmit` | `onChange`, `ranges` |
| [`controlled-state.md`](./controlled-state.md) | Pure controlled state via `ranges` + `onChange` | `ranges`, `onChange` |
| [`rtl.md`](./rtl.md) | Right-to-left via `dir` + `date-fns` locale | `locale`, `dir` |
| [`accessibility-setup.md`](./accessibility-setup.md) | Screen-reader labels via `ariaLabels` | `ariaLabels` |
| [`static-presets.md`](./static-presets.md) | Fixed date ranges (Last 7 days, This month, etc.) | `ranges` |

Each file is self-contained: one snippet + one gotcha. See
[`docs/evaluation-guide.md`](../evaluation-guide.md) for the full
install-and-evaluate journey.
